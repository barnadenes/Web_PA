function onSiteLoad() {
    clearMessages();
    showContents(['site-header', 'carousel', 'shop-body']);
}

function onShopButtonClicked() {
    clearMessages();
    showContents(['site-header', 'carousel', 'shop-body']);
}
